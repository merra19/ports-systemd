# description	: Meta port for Gnome
# depends	: glib gdk-pixbuf gtk3 gtk4 libadwaita at-spi2-core gcr
# depends	: librsvg gnome-desktop gvfs dconf gstreamer py3-gobject
# depends	: gst-plugins-base gst-plugins-good gnome-session gcr4
# depends	: gnome-settings-daemon gnome-control-center nautilus
# depends	: gnome-color-manager gnome-keyring evolution-data-server
# depends	: glib-networking gedit loupe mpv gnome-terminal cups
# depends	: adwaita-icon-theme gnome-bluetooth mutter gnome-shell
# depends	: sound-theme-freedesktop file-roller gnome-system-monitor
# depends	: gnome-shell-extensions localsearch tinysparql
# depends	: gnome-tweaks gnome-weather gnome-power-manager
# depends	: gnome-clocks gnome-calculator gnome-screenshot
# depends	:  xdg-desktop-portal-gnome gnome-disk-utility
### yelp gnome-user-docs

pkgname=gnome
pkgver=1.0
pkgrel=1
url='https://www.gnome.org/'
source=()

build() {
    install -d "$pkgdir"/usr
}
