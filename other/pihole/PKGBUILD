# description	: Pihole script
# depends	: grep bind-utils git dialog iproute2 newt procps-ng 
# depends	: make-ca fcron curl findutils sudo unzip libidn2 psmisc 
# depends	: libcap nmap jq nmap lighttpd

pkgname=pihole
pkgver=1.1.1
pkgrel=1
source=(install.sh)

build() {
    xxxxxxxxxxxx
:

    mkdir -p "$pkgdir"/opt/pihole
    install -m 0755 install.sh "$pkgdir"/opt/pihole
}