# description	: Test program for capturing input device events
# depends	: glibc xmlto py3-asciidoc

pkgname=evtest
pkgver=1.36
pkgrel=1
url='https://gitlab.freedesktop.org/libevdev/evtest'
source=(https://gitlab.freedesktop.org/libevdev/evtest/-/archive/evtest-${pkgver}/evtest-evtest-${pkgver}.tar.gz)

build() {
	cd $pkgname-$pkgname-$pkgver

    ./autogen.sh
    ./configure \
        --prefix=/usr 
    make
	make DESTDIR="$pkgdir" install
}
