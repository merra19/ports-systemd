# description	: Introspection system for GObject-based libraries
# depends	: glib meson shared-mime-info desktop-file-utils 
# depends	: libffi bison flex py3-gi-docgen

pkgname=gobject-introspection
pkgver=1.86.0
pkgrel=1
url='https://gi.readthedocs.io'
source=(https://download.gnome.org/sources/gobject-introspection/${pkgver%.*}/$pkgname-$pkgver.tar.xz)

build() {
    cd $pkgname-$pkgver

    meson setup \
        --prefix=/usr \
        --buildtype=plain \
        build
    ninja -C build
    DESTDIR="$pkgdir" ninja install -C build
}
