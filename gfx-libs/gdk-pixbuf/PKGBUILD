# description	: Image loading library for GTK+
# depends	: glib libpng libtiff libjpeg-turbo shared-mime-info
# depends	: gobject-introspection py3-gi-docgen py3-docutils
# depends	: gtk-doc

pkgname=gdk-pixbuf
pkgver=2.44.4
pkgrel=1
url='https://gitlab.gnome.org/GNOME/gdk-pixbuf'
source=(https://ftp.gnome.org/pub/gnome/sources/$pkgname/${pkgver%.*}/$pkgname-$pkgver.tar.xz
        $pkgname.hooks)

build() {
	cd $pkgname-$pkgver

	sed "/docs_dir =/s@\$@ / 'gdk-pixbuf-$pkgver'@" -i docs/meson.build

	meson setup \
        --prefix=/usr \
        --buildtype=plain \
        -D others=enabled \
		--wrap-mode=nofallback \
		-D gtk_doc=true  \
        build
	ninja -C build
    DESTDIR="$pkgdir" ninja install -C build

    install -Dm755 "$srcdir"/$pkgname.hooks "$pkgdir"/usr/share/blfs-bootscripts/hooks/$pkgname.hooks
}
