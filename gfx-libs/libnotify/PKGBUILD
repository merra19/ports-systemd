# description	: A library for sending desktop notifications
# depends	: glib gobject-introspection docbook-xml libxslt xmlto
# depends	: gtk3 py3-gi-docgen gtk-doc

pkgname=libnotify
pkgver=0.8.7
pkgrel=1
url='https://gitlab.gnome.org/GNOME/libnotify'
source=(https://download.gnome.org/sources/libnotify/${pkgver%.*}/$pkgname-$pkgver.tar.xz)

build() {
    cd $pkgname-$pkgver

	sed "/docs_dir =/s@\$@ / 'libnotify'@" \
    	-i docs/reference/meson.build 

	meson setup \
        --prefix=/usr \
        --buildtype=plain \
		-D gtk_doc=true \
        build
	ninja -C build
    DESTDIR="$pkgdir" ninja install -C build

    mv "$pkgdir"/usr/share/doc/libnotify{,-$pkgver}
}
