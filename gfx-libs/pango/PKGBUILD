# description	: Internationalized text layout and rendering library
# depends	: fontconfig freetype harfbuzz fribidi glib cairo 
# depends	: gobject-introspection xorg-proto xorg-libs 
# depends	: py3-gi-docgen help2man

pkgname=pango
pkgver=1.56.4
pkgrel=2
url='https://www.gtk.org/docs/architecture/pango'
source=(https://download.gnome.org/sources/pango/${pkgver%.*}/$pkgname-$pkgver.tar.xz)

build() {
	cd $pkgname-$pkgver

	sed "/docs_dir =/s@\$@ / 'pango-$pkgver'@" -i docs/meson.build 

	meson setup \
        --prefix=/usr \
        --buildtype=plain \
        --wrap-mode=nofallback \
        -D introspection=enabled \
		-D documentation=true \
        build
	ninja -C build
    DESTDIR="$pkgdir" ninja install -C build
}
